# Verified against PyPI: every package here ships a macOS-arm64 (or pure-python)
# wheel for Python 3.14 at its latest release, so pip won't fall back to a
# from-source build the way it did for pymupdf in backend/requirements.txt.
numpy>=2.0
pandas>=2.2
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.8
