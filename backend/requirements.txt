# Core dependencies -- required for the API, auth, DB, and risk engine to run.
# Install these first. OCR (PDF/image upload) and the optional LLM explanation
# layer are intentionally separate -- see requirements-optional.txt.
fastapi==0.115.0
uvicorn==0.30.6
sqlalchemy==2.0.35
pydantic[email]>=2.10.0,<3.0.0
python-dotenv==1.0.1
bcrypt>=4.2.0
pyjwt==2.9.0
python-multipart==0.0.9
pytest==8.3.3
httpx==0.27.2
psycopg2-binary==2.9.9
